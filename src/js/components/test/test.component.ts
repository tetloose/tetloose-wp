// import test from './test.styles.scss'

// export function sayGoodbye(name: string) {
//     return (`<div class="${test.test}"> Good bye from <span>${name}</span></div>`)
// }

export function clickBtn(elem: string) {
    return elem ? elem : 'no element sent'
}
